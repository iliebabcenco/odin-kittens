<tr>
    <td><%= kitten.id %></td>
    <td><%= kitten.name %></td>
    <td><%= kitten.age %></td>
    <td><%= kitten.cuteness %></td>
    <td><%= kitten.softness %></td>
    <td><%= link_to "Edit", edit_kitten_path(kitten.id) %></td>
    <td><%= link_to "Delete", edit_kitten_path(kitten.id) %></td>
<tr>